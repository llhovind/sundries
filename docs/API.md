# API reference

**Do not edit this file by hand.** It is generated from
`backend/config/routePermissions.js` by `npm run docs:api`. Because the app
validates that same map against the live router at every boot
(`config/validateRouteGuards.js`), this reference lists exactly the routes the
server actually serves — no more, no less — with the permission codes their
guards actually enforce.

## Conventions

- **Base URL**: all paths below are under `/api/v1` (shown in full).
- **Versioning**: the version is in the path (`/api/v1`); a breaking change
  ships as `/api/v2` beside it.
- **Auth transport**: send the access token as `Authorization: Bearer <token>`.
  Obtain it from `POST /api/v1/auth/verify-otp` (passwordless OTP) — the same
  call sets an httpOnly, rotating `refresh_token` cookie scoped to
  `/api/v1/auth`; exchange it at `POST /api/v1/auth/refresh`. Reuse of a
  retired refresh token revokes the whole token family.
- **Auth column**:
  - a permission code (e.g. `catalog:write`) — the caller's token must carry
    it; several codes joined by *or* means **any one** suffices.
  - **Public** — no session required.
  - **Session** — any signed-in user (guest or member).
  - **Session · owner** — signed-in, and the handler scopes the response to the
    caller's own rows (staff with the relevant read permission see more).
  - **Provider callback** — authenticated out-of-band (webhook signature, or the
    local fake-payment demo); not called by first-party clients.
- **Response envelope**: every response is
  `{ path, outcome: { statusCode, message, errors?, warnings?, failures? }, content? }`.
  On success the payload is under `content`; `outcome.failures` carries
  machine-readable per-item detail (e.g. per-line checkout reservation failures).


### Authentication

| Method | Path | Auth |
|---|---|---|
| POST | `/api/v1/auth/logout` | Public |
| POST | `/api/v1/auth/refresh` | Public |
| POST | `/api/v1/auth/register` | Public |
| POST | `/api/v1/auth/request-otp` | Public |
| POST | `/api/v1/auth/verify-otp` | Public |

### Catalog & storefront

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/categories` | Public |
| POST | `/api/v1/categories` | `catalog:write` |
| DELETE | `/api/v1/categories/:id` | `catalog:write` |
| GET | `/api/v1/categories/:id` | Public |
| PUT | `/api/v1/categories/:id` | `catalog:write` |
| GET | `/api/v1/products` | Public |
| POST | `/api/v1/products` | `catalog:write` |
| GET | `/api/v1/products/:product_no` | Public |
| PUT | `/api/v1/products/:product_no` | `catalog:write` |
| POST | `/api/v1/products/:product_no/image` | `catalog:write` |
| PUT | `/api/v1/products/:product_no/options` | `catalog:write` |
| POST | `/api/v1/products/:product_no/variants` | `catalog:write` |
| POST | `/api/v1/products/:product_no/variants/:variant_no/image` | `catalog:write` |
| GET | `/api/v1/promotions` | `promotions:manage` |
| POST | `/api/v1/promotions` | `promotions:manage` |
| PUT | `/api/v1/promotions/:promo_no` | `promotions:manage` |
| POST | `/api/v1/promotions/validate` | Public |
| GET | `/api/v1/search/products` | Public |

### Cart & checkout

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/cart` | Session |
| POST | `/api/v1/cart/items` | Session |
| DELETE | `/api/v1/cart/items/:variant_no` | Session |
| PUT | `/api/v1/cart/items/:variant_no` | Session |
| POST | `/api/v1/checkout` | Session |
| POST | `/api/v1/checkout/:ord_no/cancel` | Session · owner |
| POST | `/api/v1/checkout/guest` | Public |

### Orders, payments & returns

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/orders` | Session · owner |
| GET | `/api/v1/orders/:ord_no` | Session · owner |
| POST | `/api/v1/orders/:ord_no/ship` | `orders:fulfill` |
| PUT | `/api/v1/orders/:ord_no/shipments/:shipment_no` | `orders:fulfill` |
| POST | `/api/v1/payments/:ord_no/refund` | `refunds:create` |
| POST | `/api/v1/payments/fake/confirm` | Provider callback |
| POST | `/api/v1/payments/webhook/:provider` | Provider callback |
| GET | `/api/v1/rmas` | Session · owner |
| POST | `/api/v1/rmas` | Session · owner |
| GET | `/api/v1/rmas/:rma_no` | Session · owner |
| POST | `/api/v1/rmas/:rma_no/receive` | `rma:manage` |
| POST | `/api/v1/rmas/:rma_no/refund` | `refunds:create` |
| PUT | `/api/v1/rmas/:rma_no/status` | `rma:manage` |

### Inventory

| Method | Path | Auth |
|---|---|---|
| POST | `/api/v1/inventory/adjust` | `inventory:adjust` |
| GET | `/api/v1/inventory/balances` | `inventory:read` |
| GET | `/api/v1/inventory/ledger/:variant_no` | `inventory:read` |
| POST | `/api/v1/inventory/receive` | `inventory:receive` |
| GET | `/api/v1/inventory/transfers` | `inventory:read` |
| POST | `/api/v1/inventory/transfers` | `inventory:transfer` |
| GET | `/api/v1/inventory/transfers/:transfer_no` | `inventory:read` |
| POST | `/api/v1/inventory/transfers/:transfer_no/cancel` | `inventory:transfer` |
| POST | `/api/v1/inventory/transfers/:transfer_no/dispatch` | `inventory:transfer` |
| POST | `/api/v1/inventory/transfers/:transfer_no/receive` | `inventory:transfer` |
| GET | `/api/v1/inventory/warehouses` | `inventory:read` |

### Purchasing

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/purchase-orders` | `purchasing:manage` or `inventory:read` |
| POST | `/api/v1/purchase-orders` | `purchasing:manage` |
| GET | `/api/v1/purchase-orders/:po_no` | `purchasing:manage` or `inventory:read` |
| POST | `/api/v1/purchase-orders/:po_no/cancel` | `purchasing:manage` |
| POST | `/api/v1/purchase-orders/:po_no/close` | `purchasing:manage` |
| POST | `/api/v1/purchase-orders/:po_no/receive` | `inventory:receive` |
| GET | `/api/v1/vendors` | `purchasing:manage` or `inventory:read` |
| POST | `/api/v1/vendors` | `purchasing:manage` |
| GET | `/api/v1/vendors/:id` | `purchasing:manage` or `inventory:read` |
| PUT | `/api/v1/vendors/:id` | `purchasing:manage` |

### Customers & compliance

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/compliance/requests` | `customers:write` |
| POST | `/api/v1/compliance/requests` | Session · owner |
| GET | `/api/v1/customers` | `customers:read` |
| POST | `/api/v1/customers` | `customers:write` |
| GET | `/api/v1/customers/:id` | `customers:read` |
| PUT | `/api/v1/customers/:id` | `customers:write` |
| ALL | `/api/v1/customers/me` | Session · owner |
| GET | `/api/v1/customers/me` | Session · owner |
| PUT | `/api/v1/customers/me` | Session · owner |

### Reports

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/reports` | `reports:sales` or `reports:finance` or `reports:inventory` or `reports:purchasing` |
| GET | `/api/v1/reports/:slug/results` | `reports:sales` or `reports:finance` or `reports:inventory` or `reports:purchasing` |
| GET | `/api/v1/reports/:slug/runs` | `reports:sales` or `reports:finance` or `reports:inventory` or `reports:purchasing` |
| POST | `/api/v1/reports/:slug/runs` | `reports:sales` or `reports:finance` or `reports:inventory` or `reports:purchasing` |
| GET | `/api/v1/reports/:slug/runs/:run_no` | `reports:sales` or `reports:finance` or `reports:inventory` or `reports:purchasing` |
| GET | `/api/v1/reports/:slug/runs/:run_no/download` | `reports:sales` or `reports:finance` or `reports:inventory` or `reports:purchasing` |

### Administration

| Method | Path | Auth |
|---|---|---|
| GET | `/api/v1/audit-log` | `audit:read` |
| GET | `/api/v1/audit-log/entities` | `audit:read` |
| GET | `/api/v1/roles` | `roles:manage` |
| POST | `/api/v1/roles` | `roles:manage` |
| DELETE | `/api/v1/roles/:code` | `roles:manage` |
| PUT | `/api/v1/roles/:code` | `roles:manage` |
| PUT | `/api/v1/roles/:code/permissions` | `roles:manage` |
| GET | `/api/v1/roles/permissions` | `roles:manage` |
| GET | `/api/v1/settings/shipping-rules` | `settings:manage` |
| POST | `/api/v1/settings/shipping-rules` | `settings:manage` |
| PUT | `/api/v1/settings/shipping-rules/:rule_no` | `settings:manage` |
| GET | `/api/v1/settings/tax-rates` | `settings:manage` |
| POST | `/api/v1/settings/tax-rates` | `settings:manage` |
| PUT | `/api/v1/settings/tax-rates/:rate_no` | `settings:manage` |
| GET | `/api/v1/settings/values` | `settings:manage` |
| PUT | `/api/v1/settings/values/:key` | `settings:manage` |
| GET | `/api/v1/settings/warehouses` | `settings:manage` |
| POST | `/api/v1/settings/warehouses` | `settings:manage` |
| PUT | `/api/v1/settings/warehouses/:warehouse_no` | `settings:manage` |
| GET | `/api/v1/settings/weight-bands` | `settings:manage` |
| POST | `/api/v1/settings/weight-bands` | `settings:manage` |
| PUT | `/api/v1/settings/weight-bands/:band_no` | `settings:manage` |
| GET | `/api/v1/users` | `users:manage` |
| POST | `/api/v1/users` | `users:manage` |
| DELETE | `/api/v1/users/:id` | `users:manage` |
| GET | `/api/v1/users/:id` | `users:manage` |
| PUT | `/api/v1/users/:id` | `users:manage` |
| POST | `/api/v1/users/:id/roles` | `users:manage` |
| DELETE | `/api/v1/users/:id/roles/:role` | `users:manage` |
| GET | `/api/v1/users/roles` | `users:manage` |
