# Refuse to install below the `engines` floor rather than warning. Vite 8 and
# the Vitest 4 toolchain set it; an older Node fails during the build with a
# syntax or API error that points nowhere useful.
engine-strict=true
