# Refuse to install on a Node version below the `engines` floor instead of
# warning and letting it fail later at runtime. The floor is not cosmetic:
# pg-boss v12 is ESM-only and declares >=22.12.0 itself, so an install on an
# older Node produces a crash somewhere inside the job runner rather than a
# clear message here.
engine-strict=true
